---
layout: layout.njk
title: "All posts"
---

<section class="prose prose-orange dark:prose-invert">
  <h1>All posts</h1>

  {% for group, posts in collections.post | reverse | groupbyyearmonth %}
  <h3>{{ group }}</h3>
  <ul>
    {% for post in posts %}
    <li>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
    </li>
    {% endfor %}
  </ul>
  {% endfor %}
</section>