{
  "name": "blog",
  "version": "1.0.0",
  "scripts": {
    "build": "rm -r ./_site && npm run prettier && npm run tailwind && npx @11ty/eleventy",
    "prettier": "npx prettier **/*.{js,json,html,md} -w",
    "tailwind": "tailwindcss -i ./src/style.css -o ./_site/style.css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.0",
    "@tailwindcss/typography": "^0.5.10",
    "prettier": "^3.2.5",
    "tailwindcss": "^3.4.1"
  }
}
