---
layout: layout.njk
title: "All posts"
---

<header>
  <h1>{{ title }}</h1>
</header>

<article>
  {% for group, posts in collections.posts | reverse | groupByYearMonth %}
  <h3>{{ group }}</h3>
  <ul>
    {% for post in posts %}
    <li>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
    </li>
    {% endfor %}
  </ul>
  {% endfor %}
</article>
